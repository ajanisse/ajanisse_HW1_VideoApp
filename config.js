const config = {
  host : "localhost",
  port : 3000, // 3000 for windows
  user : "root",
  password : "", // blank for windows
  database: "videoapp", //The Videoapp database!
      //This will set the Kid's Mode to False by default
  kidsmode : false
};

module.exports = config;

<a href="/">
  <i class="fa fa-home"></i>
</a>

<h1>{{ title }}</h1>
<h2>{{ message }}</h2>
<ul>
  {{#each movieInfo}}
    <li>
      <h4>{{ movie_title}}</h4>
      <img src = "image/{{ imgPath }}" alt ="Populate the Image, Adam">
      <p>{{ movie_desc }}</p>

    </li>
 {{/each}}
</ul>
